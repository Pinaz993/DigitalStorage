--- Since I am used to OOP, I'll be redirecting everything here to the file that is dedicated to that object or item
--- once I have something to put there. If not, the methods will stay empty.
function access_console_define()
end

function access_console_draw()
end

function access_console_tick()
end

function access_console_change()
end